import { ThemeProp } from "@/utils";
import styled from "styled-components/native";

type ImageProp = {
    borderRadius: number;
}

const Container = styled.View`
  flex: 1;
`;


export {
    Container
}