import React from "react";
import { View, Text } from "react-native";

// import { Container } from './styles';

const Header: React.FC = () => {
  return (
    <View style={{ width: "100%", height: 200, backgroundColor: "#CDCDCD" }}>
      <Text>HEADER</Text>
    </View>
  );
};

export default Header;
